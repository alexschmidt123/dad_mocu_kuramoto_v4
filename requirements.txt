# Pinned to conda env ``mocu_optimized`` (Python 3.10.19, CUDA 12.1 / PyTorch cu121).
#
#   conda create -n mocu_optimized python=3.10 -y
#   conda activate mocu_optimized
#
# PyTorch (CUDA 12.1 wheels) — install first:
#   pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 \
#     --index-url https://download.pytorch.org/whl/cu121
#
# PyCUDA needs a CUDA toolkit matching the driver (this machine: toolkit 12.1):
#   conda install -c "nvidia/label/cuda-12.1.1" cuda-toolkit -y
#
# Then:
#   pip install -r requirements.txt
#   export PYTHONPATH="$(pwd)"

# --- core (used by src/) ---
numpy==2.2.6
scipy==1.15.2
PyYAML==6.0.3
matplotlib==3.10.7
tqdm==4.67.1

# PyTorch: prefer the cu121 wheel from the index above (env reports 2.4.0+cu121).
# Listed here so pip can satisfy already-installed torch; do not install CPU torch.
torch==2.4.0

# CUDA observation / bank generation
pycuda==2025.1.2
Mako==1.3.10
pytools==2025.2.5
platformdirs==4.5.0
siphash24==1.8
MarkupSafe==2.1.5

# torch runtime pins matching mocu_optimized (cu121)
filelock==3.19.1
fsspec==2025.9.0
Jinja2==3.1.6
networkx==3.3
sympy==1.14.0
typing_extensions==4.15.0
pillow==11.3.0
